#ifndef FACE_REC_TRAINING_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define FACE_REC_TRAINING_H

using namespace cv;

void init();
String detect_face(Mat face);

#endif
